# プッシュ実行手順（最終版）

## ✅ 確認済み
- GitHubでリポジトリが作成されている
- Personal Access Tokenをコピー済み

## 🚀 プッシュを実行

### ターミナルで以下を実行：

```bash
cd /Users/nakajima-yuusuke/Desktop/Cursor/faq-knowledge-base
git push -u origin main
```

### 認証情報の入力

1. **Username for 'https://github.com':**
   ```
   yufu8397-del
   ```
   と入力してEnter

2. **Password for 'https://yufu8397-del@github.com':**
   ```
   [コピーしたトークンを貼り付け]
   ```
   と入力してEnter
   - ⚠️ トークンは画面に表示されませんが、入力されています
   - コピーしたトークンをそのまま貼り付けてEnter

---

## 成功した場合

以下のようなメッセージが表示されます：

```
Enumerating objects: XX, done.
Counting objects: 100% (XX/XX), done.
Delta compression using up to X threads
Compressing objects: 100% (XX/XX), done.
Writing objects: 100% (XX/XX), XX.XX KiB | XX.XX MiB/s, done.
Total XX (delta X), reused 0 (delta 0), pack-reused 0 (from 0)
remote: Resolving deltas: 100% (X/X), done.
To https://github.com/yufu8397-del/faq-knowledge-base.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

これでGitHubへのアップロードが完了です！🎉

---

## 次のステップ：Renderでデプロイ

プッシュが成功したら、Renderでデプロイします：

1. **https://render.com にアクセス**
2. **「Get Started for Free」→「Continue with GitHub」**
3. **「New +」→「Web Service」をクリック**
4. **リポジトリ「faq-knowledge-base」を選択**
5. **設定を入力：**
   - **Build Command**: `npm install && cd client && npm install && npm run build`
   - **Start Command**: `node server/index.js`
   - **Environment Variables**: `NODE_ENV` = `production`
6. **「Create Web Service」をクリック**
7. **5-10分待つ**

完了！
